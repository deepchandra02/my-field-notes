name: Deploy Pages

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Determine staging ref
        id: refs
        run: |
          if [ "${{ github.event_name }}" = "pull_request" ]; then
            echo "staging_ref=${{ github.event.pull_request.head.sha }}" >> $GITHUB_OUTPUT
          else
            echo "staging_ref=dev" >> $GITHUB_OUTPUT
          fi

      - name: Checkout main (production source)
        uses: actions/checkout@v4
        with:
          ref: main
          path: prod-src

      - name: Checkout staging source
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.refs.outputs.staging_ref }}
          path: staging-src

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: prod-src/package-lock.json

      - name: Build production
        working-directory: prod-src
        env:
          VITE_BASE: /my-field-notes/
        run: |
          npm ci
          npm run build
          cp dist/index.html dist/404.html

      - name: Build staging
        working-directory: staging-src
        env:
          VITE_BASE: /my-field-notes/staging/
        run: |
          npm ci
          npm run build
          cp dist/index.html dist/404.html

      - name: Assemble combined site
        run: |
          mkdir -p site
          cp -r prod-src/dist/. site/
          mkdir -p site/staging
          cp -r staging-src/dist/. site/staging/

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: Deploy to Pages
        id: deployment
        uses: actions/deploy-pages@v4
